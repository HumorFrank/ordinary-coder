# 前端网络体系

## 网络请求封装类型
- **native（原生）**
> 基于原生 `JS API（XMLHttpRequest）`，封装网络请求
- **axios（库）**
> 基于 `axios` 库，封装网络请求
- **pinia-colada（库）**
> 基于 `@pinia/colada` 库(无需封装网络请求，注册插件后直接使用，使用方式 `hooks`使用)，封装网络请求
- **tanstack-query（库）**
> 基于 `@tanstack/vue-query` 库(无需封装网络请求，注册插件后直接使用，使用方式 `hooks`使用)，封装网络请求